/**
 * Tailwind classes for product photos (fill + object-contain).
 * Design: 1.25× zoom — PDP + quick view + gallery.
 */
export const PRODUCT_IMAGE_DISPLAY_CLASS =
  "object-contain origin-center scale-125";

/**
 * Product grid cards (home trending, shop grids) — larger zoom so shirts read bigger in tiles.
 */
export const PRODUCT_CARD_IMAGE_DISPLAY_CLASS =
  "object-contain origin-center scale-[1.4]";
